# API service: in Railway → Settings → set "Root Directory" to `saas/backend`.
# Do not set deploy.startCommand for Dockerfile builds: Railway runs it in exec form and
# `$PORT` is not expanded. Startup is defined in saas/backend/Dockerfile CMD (shell + PORT).
[build]
builder = "DOCKERFILE"
dockerfilePath = "saas/backend/Dockerfile"
